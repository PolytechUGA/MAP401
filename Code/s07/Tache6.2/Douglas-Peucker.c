#include "Douglas-Peucker.h"

Liste_Point algo_Douglas_Peucker(Liste_Point C, int j1, int j2, double *d){
    Cellule_Liste_Point pluloin = C.first->data;
    set
    for (Cellule_Liste_Point tmp = C->first; tmp->suiv != NULL; tmp = tmp->suiv){
        for (int k = j1, k<=j2, k++){
            
        }
    }
    
}